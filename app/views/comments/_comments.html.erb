<div class="comments">
  <h4>Comments (<%= commentable.comments.count %>)</h4>
  <% commentable.comments.each do |comment| %>
    <div class="comment">
      <p><strong><%= comment.user.email %></strong> commented:</p>
      <p><%= simple_format(comment.content) %></p>
    </div>
  <% end %>
</div>
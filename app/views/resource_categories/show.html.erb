<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1 class="display-4"><%= @resource_category.name %></h1>
      <p class="lead"><%= @resource_category.description %></p>
      <hr>
      <%= link_to 'Edit', edit_resource_category_path(@resource_category), class: 'btn btn-primary' %>
      <%= link_to 'Back', resource_categories_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>
